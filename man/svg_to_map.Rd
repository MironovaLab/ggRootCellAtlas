% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/svg_import.R
\name{svg_to_map}
\alias{svg_to_map}
\title{Build a map from an SVG drawing}
\usage{
svg_to_map(
  file,
  label_by = c("id", "fill", "layer", "none"),
  labels = NULL,
  skip_fills = NULL,
  closed_only = TRUE,
  curve_points = 10,
  flip_y = TRUE
)
}
\arguments{
\item{file}{Path to the SVG file.}

\item{label_by}{What names the cells in \code{ROI.name}: \code{"id"} (the shape's
\code{id}), \code{"fill"} (its fill colour, as \code{"#RRGGBB"}), \code{"layer"} (the
Inkscape layer name or the \code{id} of the enclosing group) or \code{"none"}.}

\item{labels}{Optional named vector translating those names into cell
labels, e.g. \code{c("#D1E9D1" = "Atrichoblast_e1")}. Names not in \code{labels}
are reported in a warning and get \code{NA} as \code{ROI.name}.}

\item{skip_fills}{Fill colours of shapes to leave out, such as outline or
background shapes, e.g. \code{"#231F20"}.}

\item{closed_only}{Keep only closed outlines (default). Open lines, such as
a \verb{<polyline>} or a path without \code{z}, cannot be drawn as cells.}

\item{curve_points}{Number of points used to draw each curve segment.}

\item{flip_y}{Flip the y axis, as SVG counts y downwards (default, same as
\code{ggPlantmap::XML.to.ggPlantmap()}).}
}
\value{
A data frame with one row per outline point: \code{ROI.name}, \code{ROI.id},
\code{point}, \code{x}, \code{y}, and the source of each cell in \code{svg.id}, \code{fill} and
\code{layer}.
}
\description{
Reads the cell outlines of an SVG file (for example drawn in
Inkscape or Adobe Illustrator) and returns them as a map in ggPlantmap
format, ready for \code{\link[=ggRootCellAtlas_annotation]{ggRootCellAtlas_annotation()}},
\code{\link[=ggRootCellAtlas_expression]{ggRootCellAtlas_expression()}} or \code{ggPlantmap::ggPlantmap.plot()}.
ggPlantmap itself can only build maps from Icy ROI files
(\code{ggPlantmap::XML.to.ggPlantmap()}) and cannot read SVG drawings.

Every closed outline becomes one cell (\code{ROI.id}). Supported are \verb{<path>}
(all SVG path commands, including curves and arcs), \verb{<polygon>},
\verb{<polyline>}, \verb{<rect>}, \verb{<circle>} and \verb{<ellipse>}, with \code{transform}
attributes on the shapes and their groups. Shapes inside \verb{<defs>},
\verb{<clipPath>}, \verb{<mask>}, \verb{<symbol>}, \verb{<pattern>} and \verb{<marker>} are ignored.
}
\examples{
svg <- tempfile(fileext = ".svg")
writeLines(c(
  '<svg xmlns="http://www.w3.org/2000/svg">',
  '  <path id="cellA" fill="#E69F00" d="M0 0 H10 V10 H0 Z"/>',
  '  <path id="cellB" fill="#56B4E9" d="M10 0 h10 v10 c-5 3 -5 3 -10 0 z"/>',
  '</svg>'
), svg)
map <- svg_to_map(svg, label_by = "fill",
                  labels = c("#E69F00" = "Cortex", "#56B4E9" = "Endodermis"))
ggRootCellAtlas_annotation("ROI.name", maps = map)

}
